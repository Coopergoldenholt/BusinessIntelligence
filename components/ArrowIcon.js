import React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const ArrowIcon = () => {
  return <Icon name="arrow-right" size={30} />;
};

export default ArrowIcon;
